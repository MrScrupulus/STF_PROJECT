import styles from "@/styles/components/layout/layout.module.scss";
import classNames from "classnames";

export default function Team() {
  return (
    <div className={classNames(styles.main, styles.team_page)}>
      {/* contenu de la page */}
    </div>
  );
}
